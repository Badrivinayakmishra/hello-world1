# Core Dependencies
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
gevent==24.2.1

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Authentication
pyjwt==2.8.0
bcrypt==4.1.2

# OpenAI / Azure OpenAI
openai>=1.12.0

# Vector Database
pinecone>=5.0.0

# LlamaParse
llama-parse==0.4.0
llama-index-core==0.10.43  # Pinned for compatibility with protobuf 3.20.3

# Data Processing
pandas>=2.2.0  # 2.2+ supports numpy 2.x
numpy>=2.0.0  # Required by crawl4ai, spaCy 3.8+ supports this

# Content Generation
python-pptx==0.6.23
Pillow>=10.2.0  # crawl4ai needs 10.4.0

# Office Files
openpyxl==3.1.2
python-docx==1.1.0
PyPDF2==3.0.1

# Microsoft Graph (OneDrive)
msal==1.24.1

# Video Generation
moviepy==1.0.3
pydub==0.25.1

# Integrations
boxsdk==3.9.2
slack-sdk==3.26.2
PyGithub==2.1.1
# Gmail - pinned to compatible versions
protobuf==3.20.3
google-auth==2.16.0
google-auth-oauthlib==1.0.0
google-api-core==2.11.0
google-api-python-client==2.88.0
googleapis-common-protos==1.60.0

# Azure Speech (TTS)
azure-cognitiveservices-speech==1.35.0

# AWS
boto3==1.34.19

# Utilities
python-dotenv>=1.0.0  # crawl4ai needs 1.0.1
requests>=2.31.0  # crawl4ai needs 2.32.3
beautifulsoup4>=4.12.3
httpx>=0.26.0  # crawl4ai needs 0.27.2
python-dateutil>=2.8.2

# Background Job Processing
celery==5.3.4
redis==5.0.1
kombu==5.3.4

# NLP - spaCy 3.8+ supports numpy 2.x
spacy>=3.8.0
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# ============================================================================
# OPTIONAL: Enhanced Web Scraper Dependencies
# ============================================================================
# Uncomment these lines to enable advanced web scraping features:
# - JavaScript rendering (for React/Vue/Angular sites)
# - Browser automation (Selenium or Playwright)
#
# Basic web scraping (beautifulsoup4 + requests) is already included above.
#
# For JavaScript rendering with Playwright (RECOMMENDED):
# playwright==1.40.0
# Then run: playwright install chromium
#
# For JavaScript rendering with Selenium (alternative):
# selenium==4.16.0
# webdriver-manager==4.0.1
# Then install ChromeDriver separately
#
# Note: JavaScript rendering adds 3-5x overhead. Only enable if needed.
# ============================================================================

# Crawl4AI - LLM-friendly web crawler with JS rendering
# https://github.com/unclecode/crawl4ai
crawl4ai>=0.3.0
